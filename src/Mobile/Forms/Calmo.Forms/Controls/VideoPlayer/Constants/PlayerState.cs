namespace ResourceIT.Forms.Controls.VideoPlayer.Constants
{
    using System;

    public enum PlayerState
    {
        Idle,
        Initialized,
        Prepared,
        Playing,
        Paused,
        Completed,
        Error
    }
}

